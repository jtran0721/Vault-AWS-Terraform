steps:

  - name: ":terraform: Running terraform plan"
    command: ./auto/plan
    agents:
      queue: default

  - wait

  - name: ":terraform: Deploy to prod"
    command: ./auto/apply
    agents:
      queue: default
